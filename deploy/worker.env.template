# Copy to .secrets/worker.env and chmod 600.
# This file is shell-sourced by scripts/deploy-worker.sh and scripts/worker-smoke.sh.

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
WORKER_BASE_URL=https://cf-tunnel-control-plane.example.workers.dev

# Worker runtime secrets. Use strong random values and keep them out of Git.
ADMIN_TOKEN=
AGENT_TOKEN=
SUBSCRIPTION_TOKEN=
